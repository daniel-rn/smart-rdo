@model SmartRdo.Business.Models.Area

@{
    ViewData["Title"] = "Deletar";
}

<h1>Deletar</h1>

<h3>Tem certeza de que deseja excluir o(a) Área?</h3>
<div>
    <h4>Area</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CodigoArea)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CodigoArea)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Deletar" class="btn btn-block btn-danger" />
        <a asp-action="Index" class="btn btn-block btn-outline-primary">Retornar</a>
    </form>
</div>
